namespace Thawed.UnitTests
{
    public abstract class AbstractDecodeTest
    {
        protected void AssertDecode(string bin, string op, string arg)
        {


            throw new System.NotImplementedException(bin + " / " + op + " / " + arg);
        }
    }
}